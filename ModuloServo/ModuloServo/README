To text this module you can use Key buttons 2 and 3 to change the grads and switch SW0 to choose between the two servos.


To compile the kernel module use the following command:

  make

To run the module (insert the module into the kernel):

  insmod pushbutton_irq_handler.ko

To stop the module (remove from kernel):

  rmmod pushbutton_irq_handler
  
If you want stop the previous module, make and run the new module use the script:

	./script
